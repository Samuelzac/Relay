# Rewrite dynamic routes to their static pages (URL stays the same)
/watch/*     /watch     200
/broadcast/* /broadcast 200
/success/*   /success   200
/ended/*     /ended     200

# Optional: if you ever do /admin/<anything>
/admin/*     /admin     200
